# conda env create -f environment.yml
name: lume-epics
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  - pcaspy
  - pyepics
  - numpy
  - bokeh
  - click
  - pyyaml
  - lume-model>=2.0.0
  - nose>=1.1.2
  - pip:
    - p4p
